# 开发路线图

[English](ROADMAP.md)

本文说明 `hltv-demos` 的后续开发方向，但不承诺具体完成日期。当 HLTV、
CS2、Steam 或 Agent 产品的接口变化时，优先级也可能调整。

## 当前基础：v0.3.1

当前版本已经具备：

- 从 HLTV 查找赛事，并只筛选实际打过的地图。
- 中英文地图名。
- dry-run 计划及可获取时的准确压缩包大小。
- 断点续传、响应范围和最终大小校验。
- HLTV 可信下载域名、压缩包文件名和目标路径校验。
- 压缩包完整性测试、临时解压和原子安装。
- manifest 去重及已有冲突文件保护。
- Linux x86-64 和 Windows WSL2 支持。
- Prime Agent、Claude Code、Codex、Cursor、Gemini CLI、Qoder、
  TRAE/TraeCode 安装支持。
- 可选的 WorkBuddy stdio MCP 适配器及一次性审批令牌。
- 友好 CLI、JSON 输出、环境诊断、CI 和 GitHub Release。

## 开发原则

后续版本必须继续满足：

1. 有破坏性的操作前先展示计划。
2. 赛事存在歧义时停止，不自动猜测。
3. 不覆盖已有 demo，不删除无关压缩包。
4. Agent 集成保留宿主的权限确认。
5. 下载可续传、有限制、可独立校验。
6. 只有完成真实端到端测试后，才宣布支持新平台。
7. 常规自动化测试不下载大型 HLTV 录像。

## v0.3.2：可靠性和真实客户端验证

**目标：** 在扩展新平台前补齐现有验证缺口。

计划内容：

- 完成下载、压缩包校验、解压、manifest 去重、`playdemo` 命令和安全删包
  的真实端到端测试。
- 在真实 Qoder 和 TRAE 会话中验证 Skill 发现与执行。
- 在真实 WorkBuddy 中验证 MCP 配置、权限提示、WSL 启动、令牌过期和
  单次执行。
- 给 manifest 增加跨进程文件锁和原子合并。
- 保存 HLTV HTML 测试夹具，覆盖赛事搜索、分页、比赛页面、实际地图和
  demo 重定向。
- 改进网络错误分类、退避重试、取消和修复建议。
- 测试安装器升级、回滚、中断、外来目录保护、慢网络和可选 MCP 安装。
- 增加 `verify`，检查压缩包、已安装 demo 和 manifest。

完成标准：

- 至少完成一次从计划到 `playdemo` 输出的真实下载。
- 重复执行不会重复下载。
- 多进程运行不会破坏或丢失 manifest。
- 从干净环境复现 Qoder、TRAE 和 WorkBuddy 流程。
- HLTV 测试夹具覆盖下载器使用的所有页面选择器。

## v0.4.0：原生 Windows

**目标：** 让大部分 CS2 玩家不再需要 WSL。

计划内容：

- 支持原生 Windows Python 和经过校验的 `7zz.exe`。
- 解析 Steam `libraryfolders.vdf`，发现不同盘中的 CS2。
- 正确处理盘符、UNC、空格、中文路径和长路径。
- 增加 PowerShell 安装器和 Windows Agent Skill 目录安装。
- 实现 Windows 磁盘空间、原子替换和进程锁。
- 支持从已有 WSL 安装安全迁移。
- 在安装了 Steam 和 CS2 的 Windows 上做真实测试。

完成标准：

- 新手不使用 WSL，也能完成安装、诊断、计划、下载和播放。
- Windows CI 覆盖核心与安装器测试。
- 真实 CS2 端到端验证前，不对外宣称正式支持 Windows。

## v0.5.0：新手流程和录像管理

**目标：** 用户不阅读复杂参数也能完成常见任务。

计划内容：

- 增加交互式 `wizard`，选择赛事、地图、比赛数量、目录、大小和保留策略。
- 显示速度、剩余大小、续传状态和解压进度。
- 对模糊赛事提供可选择的候选列表。
- 增加 `list`、`verify`、`clean`、`open-folder`。
- 说明如何开启 CS2 控制台并执行 `playdemo`。
- 自动选择中英文，同时允许手动指定。
- 改进磁盘不足、CS2 缺失、权限、Cloudflare 变化和压缩包损坏的提示。

完成标准：

- 第一次使用的玩家只按提示操作即可完成流程。
- 删除前完整预览所有受影响文件。
- 人类输出友好，同时保持 `--json` 对 Agent 稳定。

交互式 CLI 未稳定前不开发完整 GUI，避免维护两套不稳定流程。

## v0.6.0：公开分发与 Agent 生态

**目标：** 让用户容易发现、安装和更新可信版本。

计划内容：

- 发布到 PyPI。
- 发布校验和、SBOM 和构建来源信息。
- 提交到合适的 Agent 目录及 Qoder、TRAE 市场。
- WorkBuddy 的 `skill.yml` 导入规范足够清晰后，再研究市场包。
- 增加状态、更新和明确回滚命令。
- 制作中英文安装视频与排错指南。
- 为每个宣称支持的平台和 Agent 建立兼容性测试矩阵。

完成标准：

- 只使用 CLI 的用户不再必须 git clone。
- Release 文件及校验和可以独立验证。
- 各市场页面指向同一版本的文档和测试状态。

## 长期候选方向

以下内容尚未排期：

- 为合法、稳定的其他 demo 来源设计 Provider 接口。
- 按赛事、战队、地图、日期、标签和收藏搜索。
- demo 元数据索引和内容级重复检测。
- 支持暂停、继续的后台下载队列。
- 用 MCP 查询、验证和定位已经安装的录像。
- 当依赖和实际用途成熟后支持 Linux ARM。

## 明确不做的内容

- 绕过 HLTV 登录、付费墙、访问控制或限速。
- 为了减少提示而关闭 Agent 权限确认。
- 在没有公开本地 Skill/MCP 接口时宣称支持普通豆包；字节系编程 Agent
  使用 TRAE/TraeCode。
- 默认下载整个赛事的全部压缩包。
- 静默覆盖已有 demo、Skill 目录或无关压缩包。
- 在 CS2 本身不支持 macOS 时优先开发 macOS 安装。

## 如何跟踪

- 本文件记录产品方向和版本级验收标准。
- GitHub Issues 记录具体、可审查的任务。
- GitHub Milestones 按目标版本组织 Issues。
- 已完成的用户可见变化写入 Release Notes 或未来的 `CHANGELOG.md`，不继续
  堆在路线图中。

提交 Issue 时，请附上平台、Agent、命令、预期结果、实际输出，以及问题
是否能在 `--dry-run` 中复现。
